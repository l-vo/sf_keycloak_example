<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Let's play with Symfony authenticators: Openid connect with Keycloak</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    </head>
    <body>
        <nav class="navbar navbar-expand navbar-dark bg-dark mb-2">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('home') }}">Symfony & Keycloak</a>
                <div class="navbar-collapse collapse">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('home') }}">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('profile') }}">Profile</a>
                        </li>
                    </ul>
                    {% if app.user is not null %}
                        <span class="navbar-text">{{ app.user.userIdentifier }} (<a href="{{ path('logout') }}">logout</a>)</span>
                    {% endif %}
                </div>
            </div>
        </nav>
        {% for type, messages in app.flashes %}
            {% if type is same as 'error' %}
                {% for message in messages %}
                    <p class="alert alert-danger text-center mb-1 mx-2 p-2" role="alert">{{ message }}</p>
                {% endfor %}
            {% endif %}
        {% endfor %}
        <div class="p-5">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
